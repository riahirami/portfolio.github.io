{% extends 'admin/base.html.twig' %}
{% block title %}
	modification !
{% endblock %}

{% block body %}

	<div class="jumbotron ">


		{{form_start(form)}}
		{{form_widget(form)}}

		{#	{% for i in usr %}
													<p>{{i.username}}</p>
													<p>{{i.email}}</p>
											
											
													<hr>
											
												{% endfor %}
												#}


		<button class="btn btn-success" type="submit">editer
		</button>

		{{form_end(form)}}

	</div>
{% endblock %}
